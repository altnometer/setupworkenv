(ns user
  ;; (:require ;; [ram.configure-repl]
  ;;           [dev])
  )

;; (ram.configure-repl/-main "Loading a namespace and running the -main function")
;; (dev/-main "Loading a \"dev\" namespace and running the -main function")

;; (in-ns 'dev)
(println "Loading a 'user namespace ...")
